using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RockCollisionCheck : MonoBehaviour
{
    // void OnTriggerEnter(Collider other)
    // {
    //     if (other.gameObject.tag == "Big Rock" || other.gameObject.tag == "Small Rock") {
    //         Debug.Log("Deleted " + gameObject.name + " Hit rock: " + other.gameObject.tag);
    //         Destroy(gameObject);
    //     }
    // }
}
